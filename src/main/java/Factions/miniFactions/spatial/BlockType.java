package Factions.miniFactions.spatial;

/**
 * Enum representing the different types of blocks in the MiniFactions plugin
 */
public enum BlockType {
    CORE,
    CLAIM,
    DEFENSE,
    DOOR,
    OUTSIDE
}
